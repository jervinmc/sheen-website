nginx:
  image: nginx:alpine
  ports:
    - "80:80"
    - "443:443"
  volumes:
    - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    - /etc/letsencrypt/live/sheen-antel.homeagent.site:/etc/letsencrypt/live/sheen-antel.homeagent.site  # Mount Let's Encrypt certificates
  depends_on:
    - nuxt
  networks:
    - app-network
